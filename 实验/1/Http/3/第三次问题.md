12. 你的浏览器发送了多少条HTTP GET 请求报文？在数据包跟踪文件中，哪个数据包编号包含了获取《权利法案》的 GET 报文？

1 条；数据包编号：101

出处：请求报文的截图中，显然只能看到1条HTTP GET 请求报文；同样，可以在截图中得到此报文的编号为 101 ，请求路径为`/wireshark-labs/HTTP-wireshark-file3.html`，对应《权利法案》页面。

13. 在数据包跟踪文件中，哪个数据包编号包含了对应 HTTP GET 请求响应的状态码和状态短语？

编号112

出处：响应报文的截图中，可以看到编号为112的HTTP响应报文包含了此次请求响应的状态码和状态短语。

14. 响应中的状态码和状态短语分别是什么？

状态码：200；状态短语：OK

出处：查看响应报文`OK.txt`，帧编号 112 的 HTTP 响应头部显示`HTTP/1.1 200 OK`。

15. 承载这一条 HTTP 响应以及《权利法案》的文本内容，需要多少个携带数据的 TCP 报文段？

4 个

出处：查看响应报文`OK.txt`，帧编号 112 中标注`[4 Reassembled TCP Segments (4864 bytes): #109(1400), #110(1400), #111(1400), #112(664)]`，共 4 个携带数据的 TCP 报文段。