8. 检查你浏览器发往服务器的第一条 HTTP GET 请求内容。你在该 HTTP GET 请求中是否看到 IF-MODIFIED-SINCE 这一行？

没有

出处：查看第一次请求报文`GET2-1.txt`，HTTP 请求头中不存在`If-Modified-Since`字段。

9. 检查服务器响应的内容。服务器是否显式返回了文件内容？你是如何判断的？

服务器显式返回了文件内容

理由如下：

1. 响应报文`OK2-1.txt`中出现`Line-based text data: text/html`，说明携带了 HTML 文本内容；
2. 响应头包含`Content-Length: 371`，明确指明返回内容的字节长度。

10. 现在检查你浏览器发往服务器的第二条 HTTP GET 请求内容。你在该 HTTP GET 请求中是否看到 IF-MODIFIED-SINCE: 这一行？如果有，该头部字段后面跟着什么信息？

有；其后跟随的是文件在服务器上的最后修改时间

出处：第二次请求报文`GET2-2.txt`中存在：`If-Modified-Since: Tue, 28 Oct 2025 05:59:01 GMT`

11. 针对这第二条 HTTP GET 请求，服务器返回的 HTTP 状态码与状态短语是什么？服务器是否显式返回了文件内容？请解释原因。

返回的状态码与短语为：304 Not Modified；服务器没有显式返回文件内容

解释：

- 304 是 HTTP 缓存专用状态码，表示文件自上次请求后未被修改；
- 响应报文`NotNodified2-2.txt`中无`Line-based text data`（无 HTML 内容），也无`Content-Length`（无内容长度）；
- 服务器仅返回响应头，让浏览器直接使用本地缓存，无需重复传输文件内容。
