16. 你的浏览器一共发送了多少条 HTTP GET 请求报文？这些 GET 请求分别发往了哪些互联网地址？

浏览器一共发送了 3 条 HTTP GET 请求报文；分别发往的互联网地址：`128.119.245.12` 、`128.119.245.12`和 `2a03:4000:3e:5ca:3813:f3ff:fe33:915d`。

出处：查看报文`GET 4-1.txt`、`GET 4-2.txt`、`GET 4-3.txt`，帧 87、帧 95、帧 96 共 3 条 HTTP GET 请求；其中帧 87、帧 95 目的地址为`128.119.245.12`，帧 96 目的 IPv6 地址为`2a03:4000:3e:5ca:3813:f3ff:fe33:915d`。

17. 你能否判断浏览器是串行下载这两张图片，还是从两个网站并行下载的？请说明原因。

浏览器是并行下载两张图片。

出处：查看报文时间，请求第一张图片的帧 95 时间为 2.675320000，请求第二张图片的帧 96 时间为 2.677392000，两个请求几乎同时发送，无需等待前一张图片下载完成再发起下一个请求，因此属于并行下载。